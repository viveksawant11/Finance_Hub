import { ModuleBuildInfo } from "next/dist/build/webpack/loaders/get-module-build-info";
import React from "react";

const MobileNav = ({ user }: MobileNavProps) => {
  return <div>MobileNav</div>;
};

export default MobileNav;
